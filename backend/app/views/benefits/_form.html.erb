<%= form_with(model: benefit) do |form| %>
  <% if benefit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(benefit.errors.count, "error") %> prohibited this benefit from being saved:</h2>

      <ul>
        <% benefit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :benefit_name, style: "display: block" %>
    <%= form.text_field :benefit_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
