<%= form_with(model: scholarship_provider) do |form| %>
  <% if scholarship_provider.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scholarship_provider.errors.count, "error") %> prohibited this scholarship_provider from being saved:</h2>

      <ul>
        <% scholarship_provider.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :provider_name, style: "display: block" %>
    <%= form.text_field :provider_name %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
